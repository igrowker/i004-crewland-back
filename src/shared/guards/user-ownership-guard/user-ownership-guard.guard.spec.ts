import { UserOwnershipGuardGuard } from './user-ownership-guard.guard';

describe('UserOwnershipGuardGuard', () => {
  it('should be defined', () => {
    expect(new UserOwnershipGuardGuard()).toBeDefined();
  });
});
